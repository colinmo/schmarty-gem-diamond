CREATE TABLE Sites (url TEXT PRIMARY KEY, active INTEGER, timestamp DATETIME DEFAULT CURRENT_TIMESTAMP, profile text);
CREATE TABLE SiteChecks (url TEXT KEY, datetime TEXT DEFAULT CURRENT_TIMESTAMP, result TEXT);
